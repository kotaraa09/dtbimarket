# spec.md — dtbimarket (ขอบเขต Module 2)

ใบสั่งงานของระบบนี้ ใช้เป็นตัวตั้งให้ผู้ช่วยใน `.claude/agents/` และเป็นตัวเทียบตอนตรวจงาน
เขียนจากโค้ดที่มีอยู่จริงบน `main` วันที่ 2026-09-23 ไม่ได้เขียนจากแผน

ถ้าสเปคนี้ขัดกับ `CLAUDE.md` ให้ถือ `CLAUDE.md` เป็นหลัก ข้อมูลเชิงลึกกว่านี้อยู่ที่
`docs/01-requirements/` (อะไรและทำไม) และ `docs/02-design/` (ออกแบบอย่างไร)

> **เรื่อง SCOPE.md** — การบ้านที่ 1 ให้เขียนไว้ แต่ repo นี้ไม่มีไฟล์ชื่อนั้น
> ขอบเขตของโปรเจกต์นี้เขียนไว้แล้วใน `docs/01-requirements/feature-list.md` และ
> `product-backlog.md` ตั้งแต่ Module 1 สเปคนี้สรุปมาจากสองไฟล์นั้นกับโค้ด

---

## ระบบนี้คืออะไร

ตลาดออนไลน์กลางสำหรับธุรกิจเล็ก ๆ ที่นักศึกษามหาวิทยาลัยแม่ฟ้าหลวงเปิดเอง
และมีผู้ช่วย AI ที่อ่านข้อมูลของร้านจากระบบนี้เอง แล้วเสนอสิ่งที่ผู้ขายทำต่อได้
เป็นวิทยานิพนธ์ปริญญาโท: เครื่องมือ AI ที่อ่านข้อมูลของธุรกิจตัวเองช่วยให้เจ้าของขายดีขึ้นจริงหรือไม่

**ส่วนที่ทำเสร็จใน Module 2 คือฝั่งผู้ขายทั้งหมด** ได้แก่ สมัคร ล็อกอิน สร้างร้าน
จัดการสินค้า อัปโหลดรูป ดูภาพรวมร้าน และผู้ช่วย AI สองระดับ
ส่วนฝั่งผู้ซื้อทำไว้แค่การสมัครสมาชิก

ระบบออนไลน์: **https://dtbimarket-web.vercel.app**

| ชั้น | ใช้อะไร | อยู่ที่ไหน |
|---|---|---|
| หน้าเว็บ | Next.js (App Router), TypeScript | Vercel |
| API | Express, TypeScript | Render |
| ฐานข้อมูล | PostgreSQL ผ่าน Prisma | Neon |
| รูปสินค้า | S3-compatible | Cloudflare R2 |
| AI | OpenRouter (เรียกจาก API เท่านั้น) | — |

เบราว์เซอร์คุยกับโดเมนของเว็บเท่านั้น `/api/v1/*` ถูกส่งต่อไปที่ API
คุกกี้ session จึงเป็น first-party ตั้งเป็น `HttpOnly` และ `SameSite=Lax` (ADR-0003)

---

## บทบาทผู้ใช้

| บทบาท (`UserRole`) | ทำอะไรได้ | หน้าแรกหลังล็อกอิน |
|---|---|---|
| `seller` ผู้ขาย | มีร้านได้ 1 ร้าน จัดการสินค้าในร้านตัวเอง ใช้ผู้ช่วย AI | `/dashboard` |
| `buyer` ผู้ซื้อ | ตอนนี้สมัครและล็อกอินได้อย่างเดียว | `/account` |
| `admin` ผู้ดูแล | ดูรายชื่อผู้ใช้ ระงับหรือคืนสิทธิ์ ลบข้อมูลส่วนตัว (anonymise) ดูบันทึกการตรวจสอบ | `/admin` |

สร้างบัญชี admin ได้จาก CLI เท่านั้น (`pnpm admin:create`) หน้าสมัครสมาชิกสร้าง admin ไม่ได้

**กฎสิทธิ์ที่ห้ามละเมิด**
- ทุก route ที่เป็นของร้าน ต้องผ่าน `requireOwnStore` / `requireOwnProduct` (`apps/api/src/middleware/store-scope.ts`)
- ถ้าเปิดสินค้าของร้านอื่น ต้องตอบ **404 ไม่ใช่ 403** เพราะ 403 เท่ากับบอกว่า ID นั้นมีอยู่จริง
- ถ้าไม่ได้ล็อกอิน ต้องตอบ 401 ถ้าบทบาทผิดต้องตอบ 403 เช็กที่ API เสมอ การที่หน้าเว็บ redirect ไปหน้าอื่นไม่นับว่าป้องกันแล้ว

---

## หน้าจอ

| เส้นทาง | ใครเข้าได้ | ทำอะไร |
|---|---|---|
| `/` | ทุกคน | หน้าแรก ลิงก์ไปสมัครหรือล็อกอิน |
| `/signup` | ทุกคน | สมัครเป็นผู้ขายหรือผู้ซื้อ |
| `/signin` | ทุกคน | ล็อกอินด้วยอีเมลและรหัสผ่าน |
| `/dashboard` | seller | ภาพรวมร้าน: จำนวนสินค้า สินค้าที่ยังมีรูปน้อย และปุ่ม **ให้ AI สรุปร้านของคุณ** (AI ระดับ 2) |
| `/dashboard/products` | seller | เพิ่ม แก้ไข ลบ เผยแพร่ ซ่อนสินค้า เปลี่ยนราคา เปลี่ยนจำนวน จัดการรูป ปุ่ม **ให้ AI ช่วยเขียนคำอธิบาย** (AI ระดับ 1) |
| `/dashboard/store` | seller | สร้างร้าน (ครั้งแรก) หรือแก้ข้อมูลร้าน |
| `/account` | buyer | หน้าบัญชีผู้ซื้อ บอกว่าส่วนซื้อสินค้ายังไม่เปิด |
| `/admin` | admin | รายชื่อผู้ใช้ |
| `/admin/users/[id]` | admin | ข้อมูลผู้ใช้หนึ่งคน (ตามสิทธิ์ PDPA) ระงับ คืนสิทธิ์ และ anonymise |
| `/admin/audit` | admin | บันทึกการตรวจสอบ ว่าผู้ดูแลอ่านหรือแก้อะไรไปบ้าง |

ข้อความบนหน้าจอเป็นภาษาไทยทั้งหมด

---

## โครงสร้างข้อมูล

ชื่อตามที่อยู่ใน `packages/db/prisma/schema.prisma` ห้ามตั้งชื่อใหม่หรือเปลี่ยนรูปร่างโดยไม่มี ADR

**ผู้ใช้และสิทธิ์**
- `User`: `role` (`seller` / `buyer` / `admin`), `status` (`active` / `suspended` / `anonymised`)
- `Session`: session ฝั่งเซิร์ฟเวอร์ที่คุกกี้ชี้ไป
- `AdminAuditLog`: บันทึกการกระทำของผู้ดูแล แก้ไขหรือลบไม่ได้ (append-only)

**การค้าขาย**
- `Store`: ผู้ขาย 1 คนมีได้ 1 ร้าน (`ownerId` ห้ามซ้ำ) มี `name`, `slug`, `description`, `category`, `contactChannel`, `status` (`active` / `paused`)
- `Product`: อยู่ในร้าน มี `name` (บังคับกรอก ยาวได้ 1–120 ตัวอักษร), `description`, `priceSatang` (เก็บราคาเป็นสตางค์ ใช้จำนวนเต็ม), `stock`, `status`
- `ProductPhoto`: รูปของสินค้า เก็บ `storageKey` กับ `position`
- `Order`, `OrderItem`: มี table แล้ว แต่ยังไม่มีหน้าจอ (`OrderStatus`: `placed` → `accepted` → `ready` → `completed`, หรือ `cancelled`)

**สถานะสินค้า (`ProductStatus`)**

```
draft ──เผยแพร่──▶ published ──ซ่อน──▶ unpublished
                       ▲                     │
                       └──────เผยแพร่─────────┘
```

สินค้าที่สร้างใหม่เป็น `draft` เสมอ การเผยแพร่เป็นอีกขั้นหนึ่งที่นับแยกได้

**การวัดผล (append-only)**
- `Event`: ทุกการกระทำของผู้ใช้เขียนแถวลง table นี้ใน transaction เดียวกับการเปลี่ยนข้อมูล
  ห้าม `UPDATE` และห้าม `DELETE` มี trigger ในฐานข้อมูลคอยกันไว้
- `AiSummary`: สรุปร้านที่ AI เขียน เก็บคู่กับตัวเลขที่ใช้เขียนสรุปนั้น (snapshot)
- `AiRunLog`: บันทึกการเรียก AI ทุกครั้ง ทั้งที่สำเร็จและไม่สำเร็จ

ทุก table มี `isSeed` แถวของบัญชีตัวอย่างเป็น `true` และถูกตัดออกจากการวิเคราะห์ทุกครั้ง

---

## API (ย่อ)

ทุกเส้นทางขึ้นต้นด้วย `/api/v1` สัญญาเต็มอยู่ที่ `docs/02-design/api-spec.md`

| กลุ่ม | เส้นทาง |
|---|---|
| auth | `POST /auth/register`, `POST /auth/login`, `POST /auth/logout`, `GET /auth/me` |
| stores | `POST /stores`, `PATCH /stores/:id` |
| products | `GET /products/mine`, `POST /products`, `PATCH /products/:id`, `PATCH /products/:id/price`, `PATCH /products/:id/stock`, `POST /products/:id/publish`, `POST /products/:id/unpublish`, `DELETE /products/:id` |
| photos | `POST /products/:id/photos`, `DELETE /products/:id/photos/:photoId` |
| ai | `POST /ai/products/:id/description`, `GET /ai/summary`, `POST /ai/summary`, `POST /ai/summary/:id/dismiss` |
| admin | `GET /admin/users`, `GET /admin/users/:id`, `POST /admin/users/:id/suspend`, `.../reinstate`, `.../anonymise`, `GET /admin/audit` |

ราคา จำนวน เผยแพร่ และซ่อน แยกเป็นคนละ endpoint โดยตั้งใจ แต่ละอย่างจะได้เป็น event คนละชนิด
ถ้ารวมเป็น "บันทึก" ปุ่มเดียว การวิเคราะห์จะแยกพฤติกรรมทั้งสี่ออกจากกันไม่ได้อีก

ถ้าข้อมูลผิดรูปแบบ API ตอบ `422 validation_failed` พร้อม `details` บอกว่าผิดที่ช่องไหน

---

## ผู้ช่วย AI

| ระดับ | ทำอะไร | สิ่งที่ห้ามทำ |
|---|---|---|
| 1 เขียนคำอธิบายสินค้า | ร่างคำอธิบายภาษาไทยให้ ผู้ขายแก้เองแล้วกดบันทึกเอง | บันทึกลงสินค้าเอง |
| 2 สรุปร้าน | อ่านสินค้า รูป และ event ของร้าน แล้วเขียนสรุปกับสิ่งที่ควรทำต่อ 1 ข้อ | ใส่ตัวเลขที่ไม่มีใน snapshot ถ้ามีเลขใดไม่ตรง ระบบไม่แสดงสรุปนั้น |

คีย์ `OPENROUTER_API_KEY` อยู่ใน `.env` ของ API เท่านั้น ไม่ถูกส่งไปที่เบราว์เซอร์
ผู้ช่วย AI นี้แยกจาก **advisor** ของงานวิจัย (ADR-0007) และห้ามเอาไปใช้แทนกัน

---

## ไม่ทำใน Module นี้

| เรื่อง | เหตุผล |
|---|---|
| ฝั่งผู้ซื้อ: ดูสินค้าทุกร้าน หน้าร้าน ค้นหา ตะกร้า สั่งซื้อ (FEAT-C1–C4) | ต่อใน Module 3 |
| ผู้ขายรับออเดอร์และเปลี่ยนสถานะออเดอร์ (FEAT-C5) | ต้องมีการสั่งซื้อก่อน และต้องตัดสินคำถาม Q-5 ก่อน |
| ยอดเข้าชมและรายได้บน dashboard (ส่วนที่เหลือของ FEAT-D1) | ต้องมีฝั่งผู้ซื้อที่สร้าง event การเข้าชมก่อน |
| Advisor, `Recommendation`, `Experiment`, `Assignment` (FEAT-E*, FEAT-F2–F3) | ต้องเขียน EXP-001 ใน `docs/03-research/experiments/` **ก่อน** ลงมือเขียนโค้ด |
| สคริปต์ export และวิเคราะห์ผล (FEAT-G*) | ยังไม่มีข้อมูลจริงของช่วงศึกษา |
| เงินจริง และการชำระเงิน | ต้องถามเจ้าของโปรเจกต์ก่อนทุกครั้ง (`CLAUDE.md`) |
| ย้ายไป Firestore | ระบบนี้ใช้ PostgreSQL บน Neon เพราะงานวิจัยต้อง join `Event` กับข้อมูลร้าน และต้องใช้ trigger ในฐานข้อมูลกันไม่ให้ใครแก้ event |
