# 2026-08

2026-08-28  Requirements, product backlog and feature list written (REQ/PB/FEAT, fully cross-referenced). First user journey UJ-01 and its test spec TS-01 cover the recommendation-to-action path. Writing the journey surfaced four unresolved research decisions — the meaning of `delivered_at` being the sharpest — and the test spec turned up a design constraint: the clock has to be injectable or the 7-day boundary can never be tested.
2026-08-28  Prototype PR-01 (seller dashboard + recommendation card, clickable, events shown live beside the screen). Building it turned up three things prose had hidden: variant B needs a stored category benchmark or it cannot be delivered grounded at all; card copy must render from the frozen snapshot, not live metrics, so the card and the dashboard disagree on purpose; and researcher wording had leaked into the seller UI.
2026-08-28  Wireframe of the same screen (PR-01-wireframe.html), research instrumentation left out — three frames, nine notes, greyscale. Exported to `screenshot/` with headless Edge; the export command is in `screenshot/README.md` so the image can be regenerated instead of edited.
2026-08-28  Design set written: architecture, database schema, API spec, detailed design, plus NFRs as their own document (REQ-N17 onward) and ADR-0001 on the append-only event store. Two things fell out of writing it — price and stock need separate endpoints or their events collapse into one uncountable `product.updated`, and the advisor must render copy from templates rather than a language model or the experiment stops measuring only framing. Four decisions are blocked on the owner: hosting, auth, object storage, and that last one.
2026-08-28  D-4 decided by the owner: templates, no LLM in the advisor. Written up as ADR-0002. Knock-on — a language model API can no longer be the answer to Q-2 (the coursework external API), so object storage for product photos is now the leading candidate since D-3 needs it anyway.
2026-08-28  Submission infographic for Google Classroom — 3 A4 landscape pages, exported to PDF and PNG from one HTML source in `docs/07-delivery/`. First draft was far too text-heavy and got rewritten to tables and diagrams. Export gotcha worth remembering: headless Edge silently writes nothing if another Edge is already running, unless each run gets its own `--user-data-dir`.
