# BACKLOG — ส่งต่อ Module 3

สิ่งที่ยังไม่เสร็จเมื่อจบ Module 2 (2026-09-23) เรียงจากเรื่องที่ต้องทำก่อน
ลำดับงานเต็มของทั้งโปรเจกต์อยู่ที่ [`docs/01-requirements/product-backlog.md`](docs/01-requirements/product-backlog.md)
ไฟล์นี้คือส่วนที่ส่งต่อให้ Module 3

## 1. คีย์ AI — ย้ายไปฝั่งที่ผู้ใช้แตะไม่ได้

**สถานะ: ทำแล้ว ตั้งแต่ตอนสร้างผู้ช่วย AI (2026-09-18)**

- `OPENROUTER_API_KEY` ถูกอ่านใน `apps/api/src/lib/config.ts` บนเซิร์ฟเวอร์ Render เท่านั้น
  ในโค้ดของ `apps/web` ไม่มีคำว่า `OPENROUTER` อยู่เลย เบราว์เซอร์จึงหาคีย์ไม่เจอ แม้เปิด F12
- `.env` ถูก `.gitignore` กันไว้ ตรวจประวัติ git ทั้งหมดเมื่อ 2026-09-23 แล้ว
  ไม่มีคีย์ `sk-or-v1-…` และไม่เคยมีไฟล์ `.env` ถูก commit

**ที่ยังเหลือ**
- คีย์นี้ผู้สอนเป็นคนออกให้ และใช้ร่วมกับ repo ใบงาน `leaveeasy-anuphan` ถ้าหลุดต้องขอให้ผู้สอนยกเลิกแล้วออกคีย์ใหม่ ลบ commit ทิ้งอย่างเดียวไม่พอ
- ตอนนี้จำกัดไว้ที่ร้านละ 12 ครั้งต่อ 10 นาที (`apps/api/src/modules/ai/routes.ts`) แต่ยังไม่มีเพดานรวมของทั้งระบบ
  ถ้ามีผู้ขายจริงหลายคน วงเงินของคีย์ที่ผู้สอนให้มาอาจหมดก่อนจบช่วงศึกษา

## 2. ฟีเจอร์จาก Feature List ของ Module 1 ที่ยังไม่ได้ทำ

| ฟีเจอร์ | เรื่อง | หมายเหตุ |
|---|---|---|
| FEAT-C1, C2 | ผู้ซื้อดูสินค้าทุกร้าน หน้าร้าน และหน้าสินค้า พร้อม event การเข้าชม | **ต้องทำก่อน** เพราะถ้ายังไม่มีผู้ซื้อ ก็ยังไม่มีข้อมูลการเข้าชมหรือยอดขายให้ AI อ่าน |
| FEAT-C3 | ค้นหาและกรองตามหมวดหมู่ | Should |
| FEAT-C4 | ตะกร้าและสั่งซื้อ | ต้องตัดสินคำถาม Q-3 (ผู้ซื้อต้องมีบัญชีหรือไม่) ก่อน |
| FEAT-C5 | ผู้ขายรับออเดอร์และเปลี่ยนสถานะออเดอร์ | ต้องตัดสินคำถาม Q-5 ก่อน |
| FEAT-D1 (ส่วนที่เหลือ) | ยอดเข้าชม ออเดอร์ และรายได้บน dashboard | ตอนนี้ dashboard มีจำนวนสินค้าที่เผยแพร่ ฉบับร่าง สินค้าที่มีรูปน้อยกว่า 2 รูป และมูลค่าสต๊อก |
| FEAT-D2 | ชั้นคำนวณตัวชี้วัดที่ dashboard กับ advisor ใช้ร่วมกัน | |
| FEAT-E1–E4 | Advisor ของงานวิจัย: `Recommendation` พร้อม `metric_snapshot`, คำแนะนำเรื่องรูปสินค้า, การ์ดคำแนะนำ, ข้อความ variant ภาษาไทย | ต้องเขียน EXP-001 ก่อนเขียนโค้ด |
| FEAT-F2, F3 | `Experiment`, `Variant`, `Assignment` สุ่มกลุ่มฝั่งเซิร์ฟเวอร์ครั้งเดียว | |
| FEAT-G1, G2 | Export ข้อมูลและรันการวิเคราะห์ตามแผนที่ลงทะเบียนไว้ | ทำหลังช่วงเก็บข้อมูล |
| FEAT-H4 | คู่มือพาผู้ขายจริงเข้าระบบ | ต้องมีก่อนติดต่อผู้ขายจริง |

## 3. สิ่งที่เทสต์จับได้แต่ยังไม่ได้แก้

ทั้ง 5 เทสต์ผ่าน ([test-results.md](test-results.md)) แต่ระหว่างเขียนเทสต์เจอเรื่องเหล่านี้

- **ข้อความผิดพลาดตอนกรอกไม่ครบกว้างเกินไป** (เจอในเทสต์ข้อ 3)
  ถ้าไม่ใส่ชื่อสินค้า ผู้ขายจะเห็นแค่ "ข้อมูลไม่ถูกต้อง" ทั้งที่ API ส่ง `details.name = "กรุณากรอกชื่อสินค้า"` กลับมาแล้ว
  หน้า `apps/web/app/dashboard/products/page.tsx` แสดงแค่ `thaiMessage` แต่ไม่แสดง `details`
  แก้เสร็จแล้วให้แก้เทสต์ข้อ 3 ให้เช็กว่าขึ้นข้อความนี้จริงด้วย
- **ช่องในแผงแก้ไขสินค้าไม่มี label ผูกกับ input**
  label "ชื่อและคำอธิบาย", "ราคา (บาท)" และ "คงเหลือ" ในแถวที่เปิดแก้ไข ไม่มี `htmlFor` ผูกกับช่องกรอก
  โปรแกรมอ่านหน้าจอจึงอ่านไม่ออกว่าช่องนั้นคืออะไร และเทสต์ก็หาช่องด้วยชื่อ label ไม่ได้
  เพราะเหตุนี้การเปลี่ยนราคาและจำนวนจึงยังไม่มีเทสต์ end-to-end
- **ครั้งแรกที่เข้าหลังว่างไป 15 นาที ต้องรอเกือบหนึ่งนาที**
  API อยู่บน Render แพลนฟรีที่หลับเมื่อไม่มีคนใช้ เทสต์จึงตั้งเวลารอไว้ 120 วินาที
  ผู้ขายจริงที่เจอแบบนี้จะคิดว่าเว็บพัง ต้องแก้ก่อนเริ่มช่วงศึกษา
- **ยังไม่มีเทสต์ end-to-end ของปุ่ม AI และการอัปโหลดรูป**
  ปุ่ม AI กินวงเงินของคีย์ทุกครั้งที่เทสต์รัน ส่วนการอัปโหลดรูปจะเขียนไฟล์ลง R2 จริง ตอนนี้มีเทสต์ระดับ API ครอบคลุมสองเรื่องนี้แล้ว
- **`docs/01-requirements/feature-list.md` ไม่ตรงกับระบบจริง**
  FEAT-H1 (deploy) และ FEAT-H2 (เชื่อม API ภายนอก) ยังเขียนว่า `todo` ทั้งที่ทำเสร็จแล้ว

## 4. เรื่องของการบ้าน Module 2 ที่ต้องอธิบายไว้

- **เกณฑ์ผ่านข้อ ① บอกว่าต้องเก็บข้อมูลลง Firestore** ระบบนี้ใช้ PostgreSQL บน Neon
  เหตุผลอยู่ท้าย [spec.md](spec.md) ว่า Firestore ใช้ join `Event` กับข้อมูลร้านไม่ได้ และไม่มี trigger กันการแก้ event
  ข้อมูลเก็บลงฐานข้อมูลบนคลาวด์จริง
- **repo นี้ไม่มี `SCOPE.md`** ขอบเขตงานอยู่ใน `docs/01-requirements/` ตั้งแต่ Module 1
